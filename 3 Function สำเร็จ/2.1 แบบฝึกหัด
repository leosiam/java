แบบฝึกหัด
แบบฝึกหัดที่ 1: คำนวณค่าสัมบูรณ์
คำสั่ง: ให้เขียนฟังก์ชันที่รับตัวเลขจากผู้ใช้ และคืนค่าค่าสัมบูรณ์ของตัวเลขนั้น

==============================================================
แบบฝึกหัดที่ 2: คำนวณรากที่สอง
คำสั่ง: ให้เขียนฟังก์ชันที่รับตัวเลขจากผู้ใช้ และคืนค่ารากที่สองของตัวเลขนั้น

==============================================================
แบบฝึกหัดที่ 3: คำนวณลอการิทึมธรรมชาติ
คำสั่ง: ให้เขียนฟังก์ชันที่รับตัวเลขจากผู้ใช้ และคืนค่าลอการิทึมธรรมชาติของตัวเลขนั้น

==============================================================
แบบฝึกหัดที่ 4: คำนวณค่าไซน์
คำสั่ง: ให้เขียนฟังก์ชันที่รับค่ามุมในหน่วยองศาจากผู้ใช้ และคืนค่าไซน์ของมุมนั้น

==============================================================
แบบฝึกหัดที่ 5: คำนวณพลังงาน
คำสั่ง: ให้เขียนฟังก์ชันที่รับตัวเลขสองตัวและคืนค่าผลลัพธ์ของการยกกำลัง
==============================================================
